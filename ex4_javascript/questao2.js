// Para verificar se um número é par ou ímpar, basta dividir
// por 2, se o resto for 0 é impar
for (let i = 0; i < 16; i++){
    if(i % 2 == 0){
        document.write(i +" is even<br>");
    }else{
        document.write(i +" is odd<br>");
    }
}